# STATUS 2026-06-12 — 再開用ハンドオフ(これだけ読めば続行可能)

前提知識: APP_SPEC.md(運用仕様)、ZEROBASE_DESIGN.md(設計と全実測)、
BLOCK_RECALL_SPEC.md(A方式のアプリ実装仕様)。数値は全てログ直読で検証済み。

## 今日確定したこと

1. **A方式(BGEブロック引き戻し)正式採用・アプリ実装中**: 追い出しトークンを
   128-tokブロック+BGEベクトルで保持し、毎ターンtop-2を[SP]と[露出窓]の間に逐語再注入。
   正確値想起 0/10→7/10(4213tok)。**フルKVは同条件で1/10**(4k級では全履歴が窓内でも
   値を拾えない=露出窓+引き戻しはフルKVに勝つ)。回帰ゼロ(v1 12/12、v2 ON/OFF一致15/17)。
   実装: block_recall.py(mode="bge")+app_session_torch.py の archive 配線
   (env SPCHAT_BLOCK_RECALL=bge / SPCHAT_DEVICE=cuda)。
2. **Phase B(学習インデクサ)完成**: 教師=フル文脈でゴールド回答をteacher-forcingした
   回答位置→evictedブロックへの注意質量(BOSシンク除外・L1正規化)。生徒特徴=
   質問位置のpre-RoPEクエリ(**履歴キャッシュ込みで符号化が必須** — 素の符号化は深層で
   ドリフト)+文脈付きブロックキー(mean+max)。20万パラメータ、KL蒸留、3090で数分。
   **z(BGE)+1.5·z(indexer) アンサンブルで needle 10/10、未知テンプレ検索 top-2 .983**
   (BGE単独 .894)。生q·k照合は1/10で不可(検索ヘッド希薄性+GQA2)。
   実装: phaseb_data/labels/train.py、ensemble_archive.py(BlockArchive互換)、
   重み hypernet_sp/phaseb/phaseb_indexer.npz。
3. **B+web複合対話試験 12/14**(demo_b_composite.py、26ターン1セッション、実web):
   深い想起・L2・計算・創作ガード・雑談は全PASS。FAILは (a) verify値照応がweb検索スパムに
   負けた(既知の脆弱レグ+サンドボックスのDDG劣化)、(b)「さっき何調べたっけ」の曖昧参照が
   自由生成に落ちた — **次の修正候補: web self-log行('Earlier looked up:')を
   recallルーティングの検索対象に機械的に含める**。
4. アプリ側への回答文書: GREETING_REPLAY_SCOPE_RESPONSE.md(挨拶→断片再生は
   #15/16/20/25の守備範囲で解決済み、Block Recallの担当外。検収手順つき)。

## 残課題(優先順)

1. **V3: 逐次キー収穫** — 現在のアンサンブルは「履歴1回forward」でキー収穫(学習と同形)。
   本番ストリーミングでは生成中のrebuild forwardからk_projを拾う配線にし、その形で
   ラベル再生成→再学習→再検証。これが終わるまでBは「上限実証済み」の扱い。
2. weblog-recall の機械的修正(上記3b)と verify 照応の web 品質対策。
3. アプリ側: A方式の移植(BLOCK_RECALL_SPEC §6 パリティ手順)+ #15/16/20/25 再検収。
4. Phase C(外部ストアの学習トリガー/クエリ射影)は文献レシピまで設計済み・未着手。

## 運用メモ(ハマりどころ)

- 実行環境: /tmp/hf-sp がHFリポジトリのミラー(fft_hf はリポジトリに無い —
  build_fft_hf.py で fft_out/student.pt から再構築)。PYTHONPATH=runtime:hypernet_sp。
- RunPod: SSH不可・REST/GraphQL APIで操作(run_phaseb.sh / run_gpu_eval.sh が完成形の
  ジョブ雛形: cu126 torch==2.12.0 を実検証付きで導入、torchvision/torchaudio を必ず削除、
  CANARY_OK ゲート、HF経由で成果物回収)。**RUNPOD_POD_ID は自動注入されない** —
  pod自己削除は機能しないので必ず外から DELETE すること(ゾンビ並走で結果が汚染された)。
- fp32 1.5B のpythonを同時2本起動しない(OOM共倒れ)。pkill は自己マッチに注意。
- **計測値は必ずファイル直読で検証**(ZEROBASE_DESIGN §6: 捏造された監視通知の事象あり)。
- 生成はpythonバウンドでGPU恩恵が薄い。GPUはラベル生成・学習にだけ使う(今日の総費用~$0.9、
  RunPod残高~$9.5)。**HFトークンとRunPodキーはチャット貼付済みのため要ローテーション**。
